system_prompt = ("""Use the following context to answer the question concisely. Only include the direct answer — do not add explanations or extra commentary.
  Context: {context}
  Question: {query}
  Answer : """)